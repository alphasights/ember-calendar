<a {{action "delete" bubbles=false}} class="delete"></a>

<div class="time-interval" style={{lineStyle}}>
  {{timeInterval}}
</div>

<h1 style={{lineStyle}}>{{occurrence.title}}</h1>
<div class="resize-handle"></div>
